# Tatha 主仓配置示例（复制为 .env 后填写）
# 模型与 API（LiteLLM 用）
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# 默认模型名（LiteLLM 格式，如 openai/gpt-4o, anthropic/claude-3-5-sonnet）
TATHA_DEFAULT_MODEL=openai/gpt-4o

# 主仓 API 对外地址（助理 Tool 调用时用）
TATHA_API_HOST=0.0.0.0
TATHA_API_PORT=8000

# 数据库（与 JobFirst 主仓对齐时可复用）
# DATABASE_URL=mysql+pymysql://user:pass@localhost/jobfirst_basic
# 向量库（若用独立向量服务）
# VECTOR_STORE_URL=

# 认证（与 Zervigo 对齐时）
# JWT_SECRET=
# ZERVIGO_AUTH_URL=
